[Unit]
Description=OpenVPN metrics
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/openvpn-exporter
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
