[Unit]
Description=network metrics
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/network-exporter
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
